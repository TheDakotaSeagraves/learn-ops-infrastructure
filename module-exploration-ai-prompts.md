# learn-ops-api: AI Prompts

## 1. Project structure
"List every top-level directory in this project and briefly explain the purpose of each. Then go one level deeper into the LearningAPI directory — list the subdirectories inside it and describe what each one is responsible for."


## 2. Dependencies
"Open the Pipfile and explain what this file is and what role it plays in the project. Then locate the entries for django, djangorestframework, and django-allauth — for each one, describe what it does and why it's being used here."


## 3. Decorators, serializers, and models
"Open LearningAPI/decorators.py and explain what a decorator is and how it's being used in this file.
Then open LearningAPI/serializers.py — what do serializers do, why does a Django REST API need them, and where do they fit in the request/response cycle?
Then open the models folder inside LearningAPI. Explain what a Django model is, then pick one model and walk me through what real-world thing it represents and why the API needs to track that data."


## 4. Views, viewsets, and the MTV pattern
"Find one example of a plain view and one example of a viewset in this project — for each, give me the class name and file path.
Then explain the difference between a view and a viewset and when you'd choose one over the other.
Django follows a Model-Template-View pattern, but this project has no HTML templates. What fills that role here, and why does that approach make sense for a REST API"